steps:
    node-unit-tests:
        run:
            command: npm run test:unit
    node-pre-release-tests: 
        run:
          command: npm run test:unit {coverage}
        params:
          coverage: --coverage
core-deploy:
  enabled: true
  test-nucleus-spike : test-nucleus-spike.version